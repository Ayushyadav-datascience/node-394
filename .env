MONGO_URI=mongodb://localhost:27017/community-haven  # Replace with your MongoDB URI (local or Atlas)
JWT_SECRET=your_random_secret_key_here  # Generate a random secret key for JWT (for now, replace it with any secret key)
PORT=5000  # Port for your backend server
